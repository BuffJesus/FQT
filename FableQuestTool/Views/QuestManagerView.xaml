<Window x:Class="FableQuestTool.Views.QuestManagerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Quest Manager" Height="600" Width="900"
        MinHeight="400" MinWidth="600"
        Background="{StaticResource BackgroundPrimary}">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="{StaticResource BackgroundSecondary}" Padding="12">
            <TextBlock Text="Manage Deployed Quests" FontSize="16" FontWeight="Bold"
                       Foreground="{StaticResource TextPrimary}" />
        </Border>

        <Border DockPanel.Dock="Bottom" Background="{StaticResource BackgroundSecondary}" Padding="12,8">
            <DockPanel>
                <TextBlock DockPanel.Dock="Left" Text="{Binding StatusText}"
                           Foreground="{StaticResource TextSecondary}"
                           VerticalAlignment="Center" />
                <Button DockPanel.Dock="Right" Content="Close" Width="100" Margin="8,0,0,0"
                        Click="CloseButton_Click" />
            </DockPanel>
        </Border>

        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DataGrid Grid.Row="0" ItemsSource="{Binding Quests}"
                      SelectedItem="{Binding SelectedQuest}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      Background="{StaticResource BackgroundSecondary}"
                      RowBackground="{StaticResource BackgroundSecondary}"
                      AlternatingRowBackground="{StaticResource SurfaceBackground}"
                      BorderBrush="{StaticResource BorderBrush}"
                      BorderThickness="1">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Quest Name" Binding="{Binding Name}" Width="200" />
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80" />
                    <DataGridCheckBoxColumn Header="Enabled" Binding="{Binding IsEnabled}" Width="80" IsReadOnly="True" />
                    <DataGridTextColumn Header="File Path" Binding="{Binding FilePath}" Width="*" />
                    <DataGridCheckBoxColumn Header="Folder Exists" Binding="{Binding FolderExists}" Width="120" />
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0" HorizontalAlignment="Left">
                <Button Content="Refresh" Width="100" Margin="0,0,8,0"
                        Command="{Binding LoadQuestsCommand}" />
                <Button Content="Toggle Enable/Disable" Width="150" Margin="0,0,8,0"
                        Command="{Binding ToggleQuestCommand}" />
                <Button Content="Delete Quest" Width="120" Margin="0,0,8,0"
                        Command="{Binding DeleteSelectedQuestCommand}" />
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>
